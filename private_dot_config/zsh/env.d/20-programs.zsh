# --- Default Editor Selection ---
# Set the default terminal editor used by shell commands and CLI tools.
export EDITOR="helix"

# Set the preferred visual editor, usually used by programs that can launch
# a full-screen interactive editor interface.
export VISUAL="$EDITOR"
